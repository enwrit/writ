Add markdown-native UX, writ register, content-type directories, and rule/context templates

- Add `writ add --file` for importing markdown files and directories into .writ/
- Add `writ register` command for interactive account creation and auto-login
- Rename AgentConfig to InstructionConfig to reflect broader content types
- Route instructions to .writ/{agents,rules,context}/ based on task_type
- Add rule templates (project-rule, coding-standards) and context templates
- Add parse_markdown_file/parse_markdown_content for round-trip .mdc fidelity
- Fix YAML serialization to use literal block style for multi-line instructions
- Clean up model_dump to omit null/default fields from YAML output
- Fix `writ init --force` to properly clear stale content directories
- Improve template quality with task_type field and actionable instructions
- Add requirements.txt and requirements-dev.txt for easier setup
- Update documentation (treeview, project-rule) to reflect all changes
- 173 tests (27 new: scanner parsing, CLI --file import, store routing)
