## Type Context: Agent

This file appears to be an **agent instruction** (detected from its folder location).
Agent instructions define identity, behavior boundaries, and task scope.

Adapt your review with these agent-specific priorities:

- **Persona clarity**: Is the agent's role unambiguous? Could two people read
  this and agree on what the agent does and doesn't do?
- **Scope boundaries**: What is explicitly out of scope? Agents without clear
  boundaries tend to overreach or conflict with other agents.
- **Task delegation**: If the agent should hand off work to other agents or
  tools, are those handoff points defined?
- **Decision authority**: What can the agent decide on its own vs. what requires
  human approval? Ambiguity here leads to either paralysis or overstepping.
- **Output format**: Does the instruction specify what the agent produces?
  An agent without defined outputs generates unpredictable artifacts.
