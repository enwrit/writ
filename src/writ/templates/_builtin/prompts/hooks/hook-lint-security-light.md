## Security Quick Check

Flag any hardcoded secrets (API keys, tokens, passwords), shell injection (`eval`/`exec`/`curl|bash`), or data exfiltration patterns. Report each as `[SECURITY]` with severity (critical/high/medium). Skip if none found.
