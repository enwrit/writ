# Fix Instruction

Based on the review above, now UPDATE the file directly. Apply every fix
from "Critical Issues" and "Improvements". Apply "Quick Wins" if trivial.

Rules:
- Make surgical edits -- fix inaccuracies, don't rewrite the author's style
- Remove token waste (expert preambles, filler, redundancy) without asking
- Replace vague phrases with the concrete alternatives you identified
- Add a closure/verification block if one is missing and the file type warrants it
- Resolve contradictions by keeping the more recent or more specific version
- Preserve the file's existing structure and formatting conventions

After editing, briefly list what you changed (1-2 sentences).
